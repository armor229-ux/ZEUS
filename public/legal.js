/* ============================================================
   ZEUS - legal.js
   Shared bootstrap for the static legal pages (DMCA, Privacy
   Policy, Terms of Service). Runs the common UI initializer
   (page loader, navbar, hamburger, genre dropdown, disclaimer
   banner, back-to-top, footer year, watchlist hooks).
   ============================================================ */

'use strict';

document.addEventListener('DOMContentLoaded', () => {
  initCommonUI();
});
