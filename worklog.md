# Project Worklog

---
Task ID: 5
Agent: Z.ai Code (main agent)
Task: Fix ZEUS Party connection issues — make the party server URL user-configurable and HTTPS-safe. Only party files touched: party.html (Party Server URL input + Save above Join), party.js (localStorage-based URL with localhost default, join gating, https/http mixed-content rejection), party-server/server.js (GET /health liveness probe). No other file changed.

Work Log:
- Read worklog (Task 4) + current party files: client was hardwired to the sandbox gateway (window.PARTY_SERVER_URL="" + ?XTransformPort=5050 routing in party.js apiUrl()/connectSocket()); inline config script in party.html. Removed that approach entirely per the new spec.
- party.html: added "Party Server URL" label + input (#server-url-input) + Save button (#save-server-btn) in the join overlay above "Join the Party" (inline flex styles so party.css stays untouched); removed the old inline window.PARTY_SERVER_URL/PARTY_PORT script; bumped party.js to ?v=1.1.
- party.js: new config block — isLocalhost (localhost|127.0.0.1), DEFAULT_LOCAL_SERVER="http://localhost:5050", normalizeServerUrl (trim + strip trailing slashes), savedServerUrl (localStorage "zeus_party_server_url", try/catch), let PARTY_SERVER_URL = saved || (isLocalhost ? default : ''). apiUrl() now returns PARTY_SERVER_URL+path (no XTransformPort); connectSocket() guards empty URL and calls io(PARTY_SERVER_URL) directly; startParty validates first (disables Join + shows error); refreshJoinAvailability() live-validates on input (join disabled + join-error shown while invalid); saveServerUrl() stores to localStorage, re-normalizes, updates PARTY_SERVER_URL, toasts on success; boot() prefills the input (saved/default/''), runs validation, focuses the URL field when Join is disabled; catch message now names the exact URL; scheme check ("must start with http:// or https://"); validateServerUrl/saveServerUrl exposed on window.ZEUSParty for testing.
- Error texts exactly per spec: empty URL on non-localhost → "Enter your deployed Party Server URL (https://...) and press Save."; https page + http:// URL → "HTTPS page can't connect to HTTP server. Use an https Party Server URL."
- party-server/server.js: added GET /health → { ok: true } (liveness probe; /api/health kept as { ok, rooms }); Socket.IO CORS confirmed env PARTY_CORS_ORIGIN (default "*", comma list allowed) — verified by starting a temp instance with PARTY_CORS_ORIGIN=https://zeus.example: engine.io handshake echoed that exact origin; header comments updated (client note replaces the stale gateway note). No behavior change to rooms/sockets.
- Environment note: bash background processes are reaped between tool calls in this session (setsid/nohup/disown all die at call end — verified with sleep/node/python probes; only platform-supervised processes + agent-browser daemon persist). Workaround: started the party server at the beginning of each verification call (server state is in-memory by design, and the client's resilient rejoin recreates rooms).
- Browser-verified (agent-browser, localhost happy path): party.html input prefilled "http://localhost:5050"; Save with trailing slash → normalized + stored (localStorage zeus_party_server_url) + persists across reload; Join → room created (?room=ADZJLJ), conn "Connected" via direct io("http://localhost:5050"); chat both directions across two tabs (Alice↔Bob); invite-link joins; host migration on disconnect (system chat "Alice is now hosting"); non-host paste = local-only override (room media unchanged — host rules intact); host set_media YouTube dQw4w9WgXcQ → ytPlayer loaded + REST snapshot shows media; new joiner (Dave) received media via join state ("Paused · in sync (0:00)"); auto-rejoin after reload with ?room=; no browser errors.
- Browser-verified (HTTPS/non-localhost gating via network-route mock of https://party.test serving the real party.html/party.css/party.js — a genuine https context): boot → input EMPTY, "Enter your deployed Party Server URL (https://...) and press Save." shown, Join disabled; typing http://localhost:5050 → exact mixed-content error + Join disabled; Save stored it; https:// URL → error hidden + Join enabled; reload → saved https URL prefilled, Join enabled; scheme-less URL → "must start with http:// or https://"; join attempt with https://localhost:5050 (http-only server) → "Can't reach the Party Server at https://localhost:5050. …" + button re-enabled + conn error.
- VLM visual review of the https-error join dialog: "Party Server URL" label + input + Save on one row above Join, readable red error, no layout defects. bun run lint clean; node --check clean on all JS; dev.log has no new errors (old EADDRINUSE line predates this task). watch.html/watchparty.js untouched.
- Party server restart command (for local/production use): cd party-server && node server.js (deps already installed; env: PORT=5050 default, PARTY_CORS_ORIGIN default *).

Stage Summary:
- 3 files updated, nothing else touched: public/party.html (server-URL field + Save + v1.1), public/party.js (configurable localStorage URL, localhost default, join gating, mixed-content + scheme validation, direct absolute-URL connections), party-server/server.js (GET /health → { ok: true }, env CORS confirmed).
- Full output of the three files delivered to the user per their request.
- All connection scenarios verified: localhost default, saved URL persistence, empty URL on non-localhost (disabled + message), https+http mixed-content rejection, wrong-scheme/unreachable server error, and the complete room flow (join/chat/media/host rules/invite links) over the new direct connection.

---
Task ID: 4
Agent: Z.ai Code (main agent)
Task: Add a REAL working Watch Party system: standalone party-server/ (Express + Socket.IO, in-memory rooms, host rules, drift-corrected sync), new party.html/party.js/party.css client (YouTube IFrame API + direct MP4 + chat + invite links), and rewire the existing Watch Party button to open party.html?url=&title=. Absolute rules respected: no refactoring of the existing site; new files + minimal edits only.

Work Log:
- Read worklog (Tasks 1-3 context) + Caddyfile: gateway routes by ?XTransformPort=<port> query on ANY path (websocket upgrades included). Read examples/websocket demo: client form io("/?XTransformPort=PORT") — verified in socket.io-client source that the URL query string is preserved into opts.query and appended to EVERY polling AND websocket request.
- Key routing decision (verified empirically): engine.io's attach check is a plain path-prefix test — with server path '/' it intercepts ALL requests and kills Express on the same port. Solution: server uses the DEFAULT /socket.io path, so Express serves /api/* natively while the client still connects with the mandated io("/?XTransformPort=5050") form (the "/" in that URL is just the default namespace; the engine path stays /socket.io). curl-verified through the gateway (:81): POST /api/rooms, engine.io handshake, and /socket.io/socket.io.js bundle all return correctly.
- Created party-server/package.json (EXACT content as user specified) + party-server/server.js (CommonJS, nanoid v3): POST /api/rooms -> {roomId} (6-char look-alike-free codes), GET /api/rooms/:id, GET /api/health; socket events join/set_media/play/pause/seek/chat/get_state/leave_party; in-memory rooms {roomId, hostSocketId, media, paused, time, updatedAt, members}; first joiner = host, host migration on disconnect (with system chat + 'host' broadcast); server re-derives media type (youtube|mp4|unknown) from the URL; drift correction via time+updatedAt stamping with extrapolated snapshots on join/resync; non-host set_media/play/pause/seek rejected (party-error); empty rooms freed; resilient rejoin (unknown room ids are recreated); graceful shutdown; env PORT (default 5050) + PARTY_CORS_ORIGIN (default "*"); also serves ../public statically for standalone runs. Started with nohup node server.js on port 5050 (bun install for deps: express 4.22, socket.io 4.8.3, nanoid 3.3.18).
- Protocol test (node socket.io-client through the gateway, .scratch — removed after verification): 13/13 PASS — room create, Alice joins as host, Bob joins (members + system chat), set_media relay (youtube), non-host rejection, play relay + extrapolated state, chat both ways, pause/seek relay, REST snapshot, host migration to Bob on disconnect, migrated host sets mp4 media, non-host play ignored.
- Created public/party.css (ZEUS dark tokens: #000 surfaces, #FFD700 accent, Inter, 2-column player-left/chat-right grid, custom scrollbars, join overlay, toasts, type badges, sync pill, notice banner, tap-to-play overlay, responsive stack <880px), public/party.html (header ZEUS Party + room badge + conn pill; media card with stage + set-video form; invite card; chat card with members row + message log + entry form; sticky footer; join overlay modal with Guest-XXXX default; inline window.PARTY_SERVER_URL=""/PARTY_PORT="5050" config before party.js), public/party.js (configurable PARTY_SERVER_URL defaulting to gateway-relative '' so all requests use ?XTransformPort=5050; socket.io client loaded dynamically from the party server with CDN fallback; YouTube IFrame API loaded on demand; parseYouTubeId + .mp4 detection; host-only set_media; non-host local paste = local override that never changes room media; sync engine: non-host drift check every 1.5s, seek when |diff| > 1.2s, YT cueVideoById/loadVideoById per spec, MP4 currentTime+play/pause, hardSeek on explicit seek; host emission from MP4 play/pause/seeked events + YT onStateChange + YT seek detection (both playing AND paused — fixed a first-pass bug where paused seeks were missed); auto "Set Video" from ?url= for the host (one-shot); invite link + copy; chat rendering (XSS-safe textContent); host-hint/sync-pill/member-chips UI states; localStorage name persistence; ZEUSParty debug handle).
- Edited public/watchparty.js (the ONE existing file changed for the button): header comment updated; removed dead watchparty.me constants (WATCHPARTY_HOME, WATCHPARTY_ATTEMPT_URLS); added watchpartyResolveTitle() (#watch-title -> #watch-info-title -> document.title); watchpartyLaunch() now opens party.html?url=ENCODED&title=ENCODED in a new tab (toast "Opening your party room…"); popup-blocked fallback: copy party link + notice + redirect this tab after 2s. Button injection, URL resolution, clipboard + toast helpers unchanged. watch.html untouched (same script URL; dev mode revalidates).
- Edited eslint.config.mjs: added "party-server/**" to the existing ignores list (same pattern as examples/** — the standalone CommonJS sub-project is not part of the Next.js app; required for `bun run lint` to pass).
- Browser-verified end-to-end (agent-browser through the gateway, plus node second-clients): watch.html -> Watch Party button opens new tab party.html?url=<encoded vidcore src>&title=Fight%20Club%20(1999) with the ZEUS tab's player left intact; join overlay (Guest-XXXX default) -> room created + URL rewritten to ?room=; connected via gateway websocket; auto-set ran -> unknown-type notice shows EXACTLY "This source can't be synced automatically due to browser restrictions. Use YouTube or a direct MP4 link." while chat + manual paste keep working; host pastes YouTube URL -> Set Video -> YT iframe player loads (id dQw4w9WgXcQ) and REAL playback advances; host play/pause/seek (incl. seek-while-paused after fix) relayed to a node member; two-way chat; invite-link join (Run B): member's <video> seeked to the room's live position (~38s while host played from 30), followed host play->seek@60->pause@70 exactly ("Paused · in sync (1:10)"), tap-overlay resolved headless autoplay block; host migration on Alice disconnect (member became host: UI + system chat); MP4 host loop with a REACHABLE mp4 (MDN flower.mp4 — Google's sample bucket is 403 from this sandbox): play/seek/pause events received by members with exact times; non-host local paste: localOverride=true, room media unchanged, "Local (not synced)" pill, cleared when host set a new video; resilient rejoin after reload (same ?room auto-rejoins); sticky footer flush at viewport bottom; mobile 375px stacked layout; VLM visual review of desktop + mobile screenshots: "well-designed, defect-free"; no console/page errors; lint clean; dev.log clean.
- Cleanup: removed party-server/.scratch (test scripts/logs/screenshots). party-server left with package.json, server.js, bun.lock, node_modules, server.log. Original ZEUS files remain byte-identical to the upload (diff -rq confirms only additions in public/).

Stage Summary:
- 5 new files: party-server/package.json (exact spec content), party-server/server.js, public/party.html, public/party.js, public/party.css.
- 2 minimal edits: public/watchparty.js (Watch Party button now opens party.html?url=&title=; old watchparty.me redirect chain replaced), eslint.config.mjs (one ignore entry for the new standalone sub-project, mirroring examples/**).
- Party server runs on port 5050 (nohup node party-server/server.js; restart with `cd party-server && node server.js` or `bun run start`).
- Client↔server through the single public origin using ?XTransformPort=5050 (fetch /api/rooms, /socket.io/socket.io.js bundle, and the io("/?XTransformPort=5050") websocket); window.PARTY_SERVER_URL can override for local dev.
- Fully browser-verified: synced playback (YouTube real playback; MP4 with exact event times), chat, members, host migration, invite links, unknown-source notice, responsive dark UI. Awaiting further explicit instructions.

---
Task ID: 3
Agent: Z.ai Code (main agent)
Task: ZEUS feature round 2 (A/B/C): (A) move Login/Account entry to top-right next to search icon with avatar + dropdown; (B) Account dashboard page with per-user libraries (Likes / Favorite Movies / Watched Movies / Favorite Series) fed from existing watchlist hearts + automatic watched tracking; (C) Watch Party attempt chain (create?url → ?url → #url) with clipboard safety net.

Work Log:
- Read worklog + inspected all current files (auth.js, watchparty.js, watchlist.js, watch.html, watch.js, api.js helpers, style.css tokens, login.html/js, auth.css, nav markup on all 11 pages). Confirmed: only existing save action is the watchlist heart (.watchlist-btn, delegated handler → toggleWatchlist); no "like" feature exists; .nav-actions contains watchlist icon + search icon + hamburger on every page; script order everywhere is config → api → watchlist → [page] → auth (→ watchparty on watch.html).
- A) Rewrote the navbar section of public/auth.js: authInjectNavLink/authUpdateNavLink (nav-links <li>) replaced by authRenderNavAccount() which injects #zeus-nav-account into .nav-actions directly next to the search icon (before #hamburger). Signed out → "Login" pill (login.html). Signed in → avatar circle with email initial + caret; dropdown with email line, "Account" (account.html) and "Log out". Dropdown: toggle + aria-expanded, Escape/outside-click close, z-index 1200, glass dark styling. Styles injected once as <style id="zeus-auth-nav-styles"> from auth.js so NO existing HTML file needs a new <link> (privacy/terms/dmca untouched this round). Mobile <480px: Login pill collapses to 40px icon-only circle.
- B-1) Created public/library.js: window.ZEUSLibrary — per-user localStorage key zeus_user_library_<USER_ID> holding {likes, favoriteMovies, watchedMovies, favoriteSeries}; items {id, title, poster, type: movie|series, addedAt, year}; API getLibrary/getSection/has/add/remove/clearSection; dispatches 'zeus-library-change'. Integration with ZERO edits to existing files: wraps window.addToWatchlist / removeFromWatchlist / clearWatchlist (watchlist.js globals) so heart add/remove/clear mirrors into favoriteMovies (movie) / favoriteSeries (tv) for signed-in users — internal calls from toggleWatchlist hit the wrappers via global lookup at call time. Watched Movies: on watch.html (type=movie + numeric id + signed in) fetches getMovieDetails and adds once (deduped).
- B-2) Created public/account.html (cloned site skeleton: loader, disclaimer, navbar, footer, back-to-top; profile header + 4 section cards), public/account.js (renders email/avatar initial/Member since from zeus_users createdAt; per-section counts "(N)", poster rows with title/year/type badge/Remove; empty states; Remove mirrors back into the global watchlist via removeFromWatchlist; log out → redirect login.html; signed-out visit → redirect login.html; live re-render on zeus-library-change + storage + zeus-auth-change), public/account.css (ZEUS dark tokens; profile card, red-bar section titles like the site rows, item rows, dashed empty states, scrolling lists capped at 420px, mobile stack). Scripts on account.html: config → api → watchlist → auth → library → account.
- B-3) Updated public/login.js: Sign In / Sign Up success → toast + redirect to account.html after 600ms; Generate → credentials panel + Copy + countdown note "Redirecting to your Account page in 8s — copy your credentials now." (countdown cancels on logout) then account.html. login.html: removed the hardcoded nav "Login" <li> (the entry moved to top-right), bumped auth.js/login.js to v=3.1.
- C) Rewrote public/watchparty.js launch flow: resolve URL (video.currentSrc → video.src → #player-frame/iframe src → location.href); ordered attempt array [create?url, ?url, #url] — each opened via window.open in a new tab, first that opens wins (normally create?url=); because cross-origin playback can NEVER be confirmed from ZEUS (and DOM injection is forbidden), the clipboard safety net always runs: copy video URL + toast "Video link copied. Paste it into WatchParty."; popup opened → that tab is the redirect (ZEUS player untouched); all attempts blocked → same-tab redirect to watchparty.me after 2.6s. Button injection unchanged.
- Edited 7 HTML pages (index/browse/search/movie/tv/watchlist: auth.js?v=3.1 + library.js?v=3.0; watch.html: + watchparty.js?v=3.1). privacy/terms/dmca deliberately untouched (their auth.js?v=3.0 line still loads the updated file). Verified with diff -r vs pristine: pure line ADDITIONS only, zero modified/deleted original lines.
- Browser-verified end-to-end (agent-browser): Login pill next to search icon (DOM order + geometry <10px gap); Sign Up → auto-login → redirect to account.html; account page: avatar "T", email, "Member since August 31, 2026", 4 sections with counts + empty states; heart on movie card → global watchlist AND zeus_user_library favoriteMovies (full item shape); heart on TV → favoriteSeries type "series"; watch.html?id=550&type=movie → Fight Club auto-added to watchedMovies once (dedupe on reload); account Remove → library + watchlist sync + count/empty-state update + toast; watchlist Clear All clears favorite sections; dropdown: real click opens/stays, Account + Log out items, Escape/outside close, logout → account page redirects to login.html + nav back to "Login"; Sign In → account redirect; Generate → user-XXXXXXXX@local.zeus + 14-char password + countdown + copy line "email: ...  password: ..."; signed-out account.html visit → login.html; nav entry present on all 11 pages; Watch Party → new tab watchparty.me/create?url=<encoded vidcore URL>, clipboard.writeText called with exact video URL, ZEUS tab stays on player (iframe intact), toast shown; live inspection of the WatchParty tab: /create?url= creates a real room and tries to load the URL (player shows "Unable to play media" because embed-server URLs aren't direct media files — exactly the cross-site limitation the clipboard fallback covers). VLM visual review of desktop + mobile screenshots: account dashboard, watch player chrome with Watch Party button, mobile nav with all 4 right-side controls — no defects. node --check passed on all JS; eslint clean; dev.log clean.
- Environment notes: agent-browser cross-command clicks were flaky (page-loader overlay intercepts during the ~4–7s cinematic entrance; headless denies clipboard write/read so the copy success path was verified by instrumenting navigator.clipboard.writeText; the site's own 6s loader safety net + 1s reveal complete normally in real browsers — verified visually via screenshots once animations finished).

Stage Summary:
- 4 new files: library.js, account.html, account.js, account.css (in public/ = repo root).
- 4 updated files: auth.js (nav entry moved to top-right + dropdown), login.js (redirects to account.html + generate countdown), watchparty.js (attempt chain + clipboard safety net), login.html (removed nav Login li, version bumps).
- 7 HTML files touched with 1–3 added script lines each (index/browse/search/movie/tv/watchlist/watch). privacy/terms/dmca untouched. All diffs vs pristine are pure additions.
- Storage: zeus_users + zeus_current_user unchanged; new per-user key zeus_user_library_<USER_ID> (lazy creation, only written on first action).
- A/B/C verified working in browser. Awaiting further explicit instructions.

---
Task ID: 2
Agent: Z.ai Code (main agent)
Task: Add exactly 3 features to ZEUS (as-is, no refactoring): (1) Login page with local accounts (localStorage + Web Crypto SHA-256), (2) "Generate Local Account" button with auto-login + copy, (3) Watch Party button on the playback page redirecting to watchparty.me.

Work Log:
- Detected stack: plain HTML/CSS/JS multi-page site (classic scripts, global functions, one style.css). Followed repo patterns: page scripts call initCommonUI() at DOMContentLoaded; watchlist.js provides global showToast(); .nav-menu/.nav-links powers desktop nav AND mobile drawer; .chrome-btn styles the player-chrome buttons.
- Created public/auth.js: auth utilities — localStorage keys zeus_users [{id,email,passwordHash,createdAt}] and zeus_current_user {id,email,loginAt}; SHA-256(email+":"+password) via Web Crypto; signUp/signIn/signOut/generateLocalAccount; injects Login/Account link into #nav-menu .nav-links on every page; storage + custom-event sync.
- Created public/auth.css: login page card/tabs/inputs/messages (reusing style.css :root tokens and .search-input focus recipe) + .chrome-btn--watchparty variant (ZEUS lightning-yellow accent).
- Created public/login.html: cloned watchlist.html skeleton (loader, disclaimer, navbar with active Login link, footer, back-to-top); auth card with Sign In/Sign Up tabs, Generate Local Account button, standalone generated-credentials panel + Copy; signed-in account card with Log Out.
- Created public/login.js: tab switching, validation messages (inline, role=status), sign in/up/logout handlers, generate flow (user-XXXXXXXX@local.zeus + 12–16 alphanum password), copy single line "email: ...  password: ..."; calls initCommonUI() like every other page script (fixed the page-loader not dismissing).
- Created public/watchparty.js: injects "Watch Party" chrome-btn into #player-shell .chrome-right on watch.html; resolves video URL (HTML5 video.currentSrc/src → #player-frame src → location.href); primary: window.open watchparty.me/create?url=ENCODED (new tab keeps playback intact, opener severed); fallback when open blocked: clipboard copy (+execCommand fallback) → toast "Video link copied. Paste it into WatchParty." → redirect to watchparty.me after 2.6s.
- Edited 10 existing HTML files with ONE script line each (auth.js?v=3.0 after the last script); watch.html additionally got watchparty.js?v=3.0. Verified with diff -r vs pristine: ONLY additions, zero modifications to existing lines.
- Browser-verified end-to-end: signup validation (empty/email-format/<8/duplicate), auto-login, hash matches SHA-256(email:password) with no plaintext in storage, sign-in (wrong + correct), logout, generate (format user-8wkxocfk@local.zeus, 16-char pw, panel stays visible after auto-login), copy exact single-line format, nav link state on all 11 pages + mobile drawer, Watch Party click opened watchparty.me/create?url=... (WatchParty created a real room), ZEUS playback tab fully intact afterwards, popup-blocked fallback produced copy + exact toast + redirect. No console/page errors; dev.log clean.

Stage Summary:
- 5 new files: auth.js, auth.css, login.html, login.js, watchparty.js (all in public/ = repo root).
- 10 modified files: index/browse/search/movie/tv/watch/watchlist/privacy/terms/dmca .html — 1 added script line each (watch.html: 2 lines). No other changes to any existing file.
- Features verified working in browser. Awaiting further explicit instructions.

---
Task ID: 1
Agent: Z.ai Code (main agent)
Task: Serve the uploaded ZEUS-main.zip project exactly as it exists — no regeneration, no refactoring, no structural changes (per user's Universal Protection Prompt).

Work Log:
- Extracted /home/z/my-project/upload/ZEUS-main.zip to /tmp/zeus-extract/ZEUS-main (25 files: 10 HTML pages, 11 JS files, style.css, logo.svg, favicon.svg, robots.txt).
- Inspected the project: pure static multi-page streaming site (vanilla HTML/CSS/JS), TMDB API called client-side with key in config.js, all internal links relative (browse.html, movie.html, watch.html, etc.). No absolute-path references.
- Copied ALL ZEUS files verbatim into /home/z/my-project/public/ with `cp -a` (ZEUS's own robots.txt/logo.svg replaced scaffold placeholders).
- Verified integrity: `diff -r` confirms public/ files are byte-identical to the uploaded project — ZERO modifications to any ZEUS file.
- Added a single beforeFiles rewrite in next.config.ts: `/` → `/index.html` so the root URL serves ZEUS's own homepage byte-for-byte (no URL change, no React wrapper, no hydration layer).
- Dev server auto-restarted on config change (see dev.log: "Found a change in next.config.ts. Restarting...").
- curl-verified: `/` returns ZEUS index.html (29898 bytes, diff-identical); /browse.html, /movie.html?id=550, /style.css, /config.js all HTTP 200 with correct content types.
- Browser-verified end-to-end with agent-browser:
  - `/` renders fully: hero carousel ("Lanterns", 5 slides), TOP 10 Today, Trending Now rows with live TMDB data.
  - Nav "Movies" → /browse.html?type=movie renders "Popular Movies" grid.
  - Clicked poster → /movie.html?id=969681 renders Overview / Top Cast / Similar Movies.
  - "Watch Now" → /watch.html?id=969681&type=movie renders player iframe + server selector.
  - Watchlist: add → /watchlist.html shows persisted entry.
  - Search: /search.html live search for "Dune" returns real results.
  - Legal pages: /privacy.html, /terms.html, /dmca.html load.
  - Footer flush with document bottom (flushWithDocBottom: true); mobile (375x667) and desktop (1280x800) views OK.
  - No page errors, no console errors, no hydration issues.

Stage Summary:
- ZEUS is served exactly as uploaded: static files in public/ + one URL rewrite in next.config.ts. No ZEUS file was touched, renamed, refactored, or rewritten.
- src/app/page.tsx (scaffold) is shadowed by the rewrite and never renders at `/`; it was intentionally left untouched.
- Third-party ad scripts included in the original project were left in place as-is.
- Awaiting explicit user instructions before any modifications.
